<Window x:Class="PdTracker.Desktop.Views.ConnectionSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PD Tracker - Database Setup"
        Height="280" Width="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Database Connection"
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1"
                   Text="Enter the connection string for your SQL Server Express instance."
                   Foreground="#666" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="Server:" Margin="0,0,0,4"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ServerTextBox" Grid.Column="0"
                     Text="{Binding ServerName, Mode=TwoWay}"
                     ToolTip="e.g. localhost\SQLEXPRESS or 192.168.1.10\SQLEXPRESS"/>
            <StackPanel Grid.Column="1" Margin="12,0,0,12" VerticalAlignment="Center">
                <CheckBox x:Name="TrustedCheckBox" Content="Trusted Connection (Windows Auth)"
                          IsChecked="{Binding UseTrustedConnection, Mode=TwoWay}" FontSize="13"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Test Connection" Click="TestConnection_Click"
                    Padding="16,8" Margin="0,0,12,0"/>
            <Button Content="Connect" Click="Connect_Click" IsDefault="True"
                    Padding="24,8" Background="#2D5A8A" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
